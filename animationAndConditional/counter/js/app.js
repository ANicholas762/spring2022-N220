let count = 0; // declared variable storing the value of 0.

function draw() {
   count++; // increases the count by 1 each frame
   console.log(count); // outputs the counting values to the console log on the live server
}